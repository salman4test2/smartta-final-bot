[tool:pytest]
# Configure pytest to only use asyncio backend and avoid trio
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = --strict-markers --disable-warnings
markers =
    asyncio: marks tests as requiring asyncio

# Disable anyio trio backend since trio is not installed
[anyio]
backends = asyncio
